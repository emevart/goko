---
status: living
area: process
updated: 2026-09-13
---

# NOW: где проект сейчас

Живой снимок: фокус, открытые вопросы, следующий шаг. Агент обновляет в конце
каждой сессии, founder — когда меняет курс.

## Фокус

Стадия 1, план ядра, закрыт 13.09: задачи 1–13 и пакет правок ревью 12b
`[OK]`; задача 13 — после ревью, правок ядра по её находкам и круга правок по
ревью. Работа возобновлена 13.09 по решению founder'а. Есть `go-core`,
`protocol`, `go-engine`, `game-server` (сессии, токены LiveKit по D-0001, HTTP
и SSE), `npm run smoke` (сценарная партия с фейковым движком, `--real` против
KataGo) и `npm run dev`. Спека сверена с кодом, решения D-0002…D-0010 записаны.
Ход задач и находки — `journal/2026-09-08-stage1-core.md`.

Работа идёт в ветке `stage0`; `main` отстала на всю стадию 1, слияние — после
широкого ревью ветки.

Стадия 0 закрыта по всем задачам, которые агент может выполнить без микрофона.
Результаты — `research/stage0-results.md`, ход задач — `journal/2026-09-08-stage0.md`.
Осталось одно: голосовой прогон с телефона, его делает founder.

Планы реализации лежат в `docs/superpowers/plans/`:

- `2026-09-07-goko-stage0-spike.md` — стадия 0: `infra/`, стартовый агент,
  замеры KataGo, фразы, текстовый канал, `docs/research/stage0-results.md`.
- `2026-09-07-goko-stage1-core.md` — стадия 1, ядро: `go-core`, `protocol`,
  `go-engine`, `game-server`, `smoke`, `dev` (13 задач).
- `2026-09-07-goko-stage1-voice-web.md` — стадия 1, голос и веб:
  `voice-agent`, `scripts/chat.mjs`, `web`, контейнеры, runbook (10 задач).

Корпус голосовых проверок: `spike/phrases.md`, 118 фраз с шести записей
founder'а, разрезаны по словам в `data/voice/phrases/` (вне git — голос
founder'а, репозиторий публичный).

## Инфраструктура `[OK]` (08.09)

- VPS `goko`: Hetzner cx23 (2 vCPU, 4 ГБ, 40 ГБ, AVX2 есть), Хельсинки,
  Ubuntu 24.04, отдельный проект Hetzner `goko`; SSH-алиас `goko` на ПК
  founder'а. Токен API Hetzner — в `.env` на ПК как `HETZNER_API`, не в git.
- Домены: `goko.sdamex.com` (страница и `/api`) и `goko-lk.sdamex.com`
  (LiveKit), A-записи в Yandex Cloud DNS зоны `sdamex.com`, TTL 300;
  правятся с ПК founder'а через `yc dns zone list-records / add-records`.
  В `.env` это `WEB_HOST` и `LK_HOST`.
- Развёрнуто 08.09: Docker, ufw (в том числе relay-порты TURN 30000-40000/udp),
  `/opt/goko` с `.env` режима 600, Caddy и LiveKit `v1.13.6` в compose,
  сертификаты Let's Encrypt до 07.12.2026. Оба контейнера `Up (healthy)`,
  `https://<LK_HOST>/` отдаёт `OK`. Статики ещё нет: на `<WEB_HOST>` заглушка.
- Ключи LiveKit сгенерированы на сервере, ключ OpenAI вписан в `/opt/goko/.env`.
- Репозиторий `github.com/emevart/goko`, публичный, лицензия MIT.
- KataGo на ПК: бинарь OpenCL v1.18.1 и обе сети скачаны, `KATAGO_BIN` в `.env`.

## Открытые вопросы founder'у `[TODO founder]`

1. **`[OK]` Ключ OpenAI перевыпущен** founder'ом 08.09 и разложен в `.env` на
   ПК и на сервере, доступ проверен живым запросом. Инцидент закрыт.
2. **Голосовой прогон с телефона — блокер стадии 1.** Одна сессия закрывает
   сразу четыре пустых места в `research/stage0-results.md`: проходит ли
   медиапоток из сотовой сети и из дома без VPN, доля распознанных координат
   из 45 фраз (`spike/phrases.md`), качество русского голоса Realtime и
   перебивание, стоимость минуты. Пошаговая инструкция — раздел «Что делать
   founder'у с телефоном» в отчёте выкатки спайка. Страница на сервере обновлена
   08.09: до этого она не показывала реплики агента, теперь показывает. Если поток душат — RU-релей
   или LiveKit на RU-VPS, раздел 11 спеки.
3. **`[OK]` Почта для Let's Encrypt вписана** founder'ом 09.09: `ACME_EMAIL` в
   `/opt/goko/.env` заполнен, контейнер Caddy пересоздан, оба хоста отвечают 200.
   Значение в git не попадает. Текущие сертификаты выписаны на аккаунт ACME без
   почты; новый аккаунт с почтой Caddy заведёт при продлении (до 07.12.2026).
4. **Tailscale** нужен только для dev-режима стадии 1 (Caddy на VPS →
   game-server на ПК). Когда дойдёт: агент выполнит `tailscale up` на VPS и
   пришлёт ссылку для входа, founder откроет её и подтвердит узел.
5. **`[OK]` Правки спеки 07.09 подтверждены** founder'ом 13.09: отдельный VPS вместо LiveKit Cloud на
   стадии 0; личный VPN-сервер не используем; запасной конвейер STT → LLM →
   TTS; раздел 18 про AI-first; коды ошибок и причины `state.updated`.
6. **`[OK]` Способ исполнения стадии 1** выбран 08.09: субагентами, начиная с
   плана ядра.
7. **Две неоднозначные фразы** в `spike/phrases.md`: строка 61 «у меня всё ок» и
   строка 110 «как ходить». Ожидание проставлено догадкой; если founder помнит,
   что имел в виду, поправим на настоящее. Работу не держит.

## Открыто

- `[TODO]` Сервису game-server в compose — `stop_grace_period` больше 25 с,
  когда сервис туда добавят (D-0010).
- `[TODO]` Когда game-server встанет за Caddy — проверить живым потоком, что
  `flush_interval -1` в `infra/Caddyfile` отдаёт события SSE сразу.
- `[TODO]` После деплоя голоса — живым прогоном проверить, что комната с одним
  агентом закрывается по `empty_timeout` (D-0001). `npm run smoke` комнат не
  создаёт и этого не проверяет.
- `[TODO]` Детям `smoke` и `dev` уходит всё окружение, кроме `LIVEKIT_*` и
  `OPENAI_*` в smoke (например, ключ API облака). Значения не печатаются;
  белый список переменных по принципу наименьших прав не сделан.
- `[TODO]` POSIX-ветки `smoke` и `dev` (дети в своих группах процессов, дерево
  гасится `process.kill(-pid)`) ни разу не прогонялись: проверить на Linux или
  macOS.
- `[TODO]` Когда появится `apps/web` — проверить Ctrl+C в `npm run dev` для
  `web`, который запускается через `cmd.exe` (`shell: true`): возможен вопрос
  «Завершить выполнение пакетного файла?».
- `[!]` `dev` на Windows: дети получают Ctrl+C одновременно с `dev`. Если все
  они выйдут раньше, чем сработает обработчик SIGINT в `dev`, `dev` напечатает
  `[X] dev: все процессы завершились` и выйдет с кодом 1. В замере обработчик
  успел первым; код не меняли.
- `[TODO]` Широкому ревью: `create` не проверяет, занят ли id (D-0009).
- `[TODO]` Отложено: комната LiveKit остаётся после таймаута `createRoom`.
- `[TODO]` Общий таймаут прогона в CI (решение задачи 10), когда появится CI.
- `[!]` Косметика: лог stderr KataGo рвётся посреди строки (префикс на кусок).

## Следующий шаг

Широкое ревью ветки `stage0` и слияние `stage0` → `main` по решению founder'а.
Затем, после «поехали», план голоса и веба субагентами; деплой — после него.
После плана — голосовой прогон founder'а с телефона: одна сессия закрывает и
пустые места стадии 0, и приёмку стадии 1.

Осталась задача 8 стадии 0 — удалить `spike/` и вычистить его из `tsconfig.json`
и `workspaces`; делать её после прогона, не раньше.

## Сделано

- 2026-09-07: brainstorming, спека v1, правки по инфраструктуре и речи,
  `CLAUDE.md`, `AGENTS.md`, `README.md`, карта документации.
- 2026-09-07: планы реализации стадии 0 и стадии 1 (ядро; голос и веб);
  в спеке уточнены коды ошибок и причины `state.updated`.
- 2026-09-08: организация работы через субагентов (`process/orchestration.md`),
  каркас монорепы и `doctor` (задача 1), внешняя верификация планов до кода.
- 2026-09-08: VPS `goko` (cx23) и A-записи `goko.`/`goko-lk.` в `sdamex.com`;
  LICENSE MIT, публикация репозитория; в планах и спеке `WEB_HOST`/`LK_HOST`
  вместо `DOMAIN`, cx23/cx33 вместо CX22/CX32, риск троттлинга и запасной
  RU-релей в разделе 11.
- 2026-09-08: широкий обзор ветки `stage0`, шесть исправлений, ре-ревью чисто;
  исправленная страница спайка выкачена. Предполётная сверка плана ядра до
  написания кода: шесть блокирующих дефектов плана и девятнадцать замечаний,
  решения вынесены, правки в план идут.
- 2026-09-08: стадия 0, задачи 1-5 и 7. `infra/` и подъём VPS (Docker, ufw,
  Caddy, LiveKit, TLS), образ движка и замер KataGo на сервере, агент-спайк на
  `gpt-realtime` с выкаткой и живой комнатой, текстовый канал `chat.mjs`,
  `research/stage0-results.md` с решениями. В спеке зафиксированы основная
  сеть `b10c128`, тариф cx23 без апгрейда, режим голоса `realtime` с
  параметрами `server_vad`, relay-порты TURN. Задача 6 (стоимость и запасной
  конвейер) на паузе до голосового прогона.
- 2026-09-13: стадия 1, ядро закрыто — задачи 8 и 10 после ре-ревью, 11
  (сессии, токены LiveKit, D-0001), 12 (HTTP, SSE, запуск), пакет правок 12b,
  13 (`npm run smoke`, `npm run dev`, сверка спеки с кодом, D-0002…D-0010).
- 2026-09-13: ревью задачи 13 и правки по нему. Починены остановка game-server
  при медленном клиенте SSE и пустые переменные env. Ctrl+C в `npm run dev` на
  Windows теперь доходит до детей. `smoke` не ждёт `/health` у вышедшего
  процесса, а Ctrl+C в `smoke` считается ошибкой и дожидается остановки детей.
  go-engine отклоняет неверный `ENGINE_PORT`. Уточнены спека (в том числе
  остановка game-server со счётчиком запросов), D-0001, D-0005, D-0006 и D-0010.
