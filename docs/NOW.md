---
status: living
area: process
updated: 2026-09-16
---

# NOW: где проект сейчас

Живой снимок: фокус, открытые вопросы, следующий шаг. Агент обновляет в конце
каждой сессии, founder — когда меняет курс.

`[OK 16.09 опубликовано 422edb5]` Переработанный цельный орб выложен в production:
WebGL canvas теперь круглый и прозрачный, внешние кольца и частицы удалены,
деформация сферы реагирует на активный голосовой канал, объём добавляют
светотень, backlight, блик и внутренняя структура, состояния переходят плавно.
После deploy проверены web `200`, LiveKit `OK` и пять healthy-контейнеров.

`[FIX 16.09 в работе]` Разобрана последняя партия на ревизии 47: серверная доска
и ходы согласованы, а расхождение возникло при пересказе ASCII-макета моделью.
В `get_position` и фоновый контекст добавлен полный список камней по цветам и
готовое русское произношение; промпт требует считать его источником истины.
Шумовые метки STT теперь скрываются как отдельные реплики и форматируются в
смешанной фразе (`*вздох*`, `*кашель*`), а шум не перебивает текущую речь.
Орб получил отдельный визуальный режим работы инструментов и атрибут
`goko.tool`, различая слушание, размышление, речь и проверку доски.
Составные голосовые просьбы теперь подтверждают разные действия одной реплики,
а вызовы инструментов выполняются последовательно. `undo` и `redo` принимают
число порций (1–8) и повторяют операцию с новой ревизией после каждого шага;
для «несколько» используется две порции. Проверка: `npm run check` — 1569
passed / 13 skipped; web build — PASS. Сверка намерения теперь терпит короткую
разговорную `user_utterance`, если координата или смысловой якорь совпадают, но
не пропускает отрицания и не оживляет старую команду. Живой 3D-орб реализован
по [спеке](superpowers/specs/2026-09-16-orb-visual-design.md) и
[плану](superpowers/plans/2026-09-16-orb-visual-design.md): Three.js-сцена
загружается только в голосовом режиме, переходы между семью состояниями
интерполируются, reduced-motion и отсутствие WebGL оставляют CSS-fallback.
Микрофонный и удалённый уровни разделены, поэтому орб слушает человека и
говорит за Гоко независимо; canvas освобождается при завершении разговора.
По новой обратной связи founder'а визуал переработан: кольца и частицы удалены,
canvas принудительно ограничен круглым силуэтом, а поверхность получила
многослойную шумовую деформацию с отдельной реакцией на голос и плавным
smoothstep-переходом состояний. Внутреннее освещение, backlight, мягкий
спекулярный блик и зернистая структура дают сфере объём, а уровень речи ещё
слегка меняет общий масштаб орба.
Кнопка диалога показывает `200+`, когда достигнут текущий буфер последних 200
реплик, чтобы не выдавать его за общий счётчик; это UI-ограничение, не лимит
LiveKit или модели. Вызовы сверки позиции
помечены внутренними: голос не должен объявлять их на каждом ходу, орб показывает
состояние сам; `дай подумать` оставлен только для заметно долгой операции.
Подробности: [разбор последней партии](research/2026-09-16-last-party-state-review.md).
[Составные команды](research/2026-09-16-multi-action-intents.md).

## Фокус

`[OK 15.09 опубликовано d3d82cf]` Русское произношение и фоновый контекст доски.
Независимый SSE-наблюдатель передаёт актуальные факты и замысел, анализируется
устойчивая ревизия, поздние результаты отбрасываются. Полный check: 1535 passed /
13 skipped. Реальный Live принял обновление без речи (нет текста, пик фона
52/32768), API-ошибок нет. На production записанный D4 дал D4/K10; вопрос о числе
ходов и последнем ходе получил верный ответ; отмена прошла. Проверочная партия
завершена, комната закрыта. Пять сервисов healthy, публичные артефакты и исходники
контейнера совпали с архивом коммита. Из игровых сервисов перезапущен только voice-agent.
[Итог и следующие этапы](research/2026-09-15-stage1-wrapup.md),
[решение D-0016](decisions/0016-background-board-awareness.md).
Открыты прерывания/кашель, редкие вводные, полная слуховая приёмка русского,
долговременное хранение замысла и замеры стоимости. Следующий шаг — живая приёмка
этой версии и затем голосовые регрессии; MCP/аккаунты/история партий описаны в итоге.

`[OK 15.09 опубликовано 25a0a54; голосовая приёмка частичная]` Итерация личности и интерфейса:
единый composer, живые промежуточные расшифровки, порядок по началу реплики,
выбор пересечения удержанием/перетаскиванием, Stone (выбран founder по образцам),
экспериментальный выбор LLM из легальных кандидатов KataGo. Реальный API-прогон
на двух позициях завершён без ошибок; полный check: 1530 passed / 13 skipped.
История и диагностика перенесены в верхние кнопки, панели закрываются отдельным
крестиком, повторным нажатием, Escape или нажатием снаружи. В браузере проверено
открытие/закрытие обеих панелей; текущая партия founder с шестью ходами сохранена.
По явной просьбе founder опубликован `25a0a54`: пять сервисов healthy,
VOICE_MODE=live, GOKO_VOICE=stone, GOKO_PLAYER_MODE=persona. HTML, оба публичных
артефакта и исходники контейнеров сверены с архивом коммита. На production прошли
текстовый K4 и отмена; записанная команда D4 в паузе дала два хода, серверный
журнал подтвердил выбор persona. Отдельный запрос из контейнера также вернул
валидный выбор модели (HTTP 200, 2.6 с). Одна попытка использовала engine fallback.
Незакрытый дефект: K4, произнесённый поверх ответа об отмене, распознан и повторён
голосом, но инструмент не вызван. Полный аудиопрогон остановлен на этом этапе;
это не полная приёмка прерываний. Проверочные комнаты закрыты и их партии завершены.
Локальный preview на :5173 используется founder, его сеанс и сервер не закрывать
как тестовые. Следующий шаг: исправить и проверить делегирование новой команды
при наложении речи и реакцию на кашель; продолжить
[план](superpowers/plans/2026-09-15-goko-personality-ux.md).

`[FIX 15.09 опубликовано 2c31751]` Последующая реальная реплика «мой первый ход чёрными К4»
выявила недостаточность предыдущей приёмки: модель правильно вызвала K4,
но grammar whitelist отклонил реплику. По прямой просьбе founder whitelist
удалён для всех мутаций; понимание речи передано модели, источник/повторы/
координаты и серверные правила проверяются кодом. Известные шумовые метки
очищаются в пользовательской ленте и ledger. Полный check: 1517 passed /
13 skipped. Семантические примеры перенесены из regex unit в платный eval
реальной модели: 29/29 PASS. На опубликованной версии 11 игровых этапов
прошли через реальные room/audio/SSE; все 10 tools видны в серверном журнале.
Оценка дополнительно проверена отдельным сеансом до завершения ответа.
Тестовые партии/сеансы закрыты. Пять сервисов healthy, hashes контейнера и
публичных артефактов совпали. [Отчёт](research/2026-09-15-natural-intent-recovery.md).
Следующий шаг: обновить страницу и включить голос; шумовые пометки из примера
очищаются, но реакция на настоящий кашель остаётся поведением GPT-Live.

`[OK 15.09]` После повторных жалоб выполнено [сквозное ревью регрессий](research/2026-09-15-live-regression-review.md).
Опубликован `eb518f6`: восстановлены разговорные команды и сопоставление STT,
сохранение настроек новой партии, управляемый микрофон и состояние reconnect.
Финальный check: 1522 passed / 13 skipped. Настоящий аудиопрогон: 8 записей,
11 этапов PASS, проверены все 10 инструментов и изменения доски через SSE.
Браузер Codex: запись D4 → доска/ответ K11 после mute/unmute и reconnect;
кнопки отмены/возврата и завершение PASS. Пять сервисов healthy, артефакты сверены.
Проверочные партии/сеансы закрыты, локальный Vite остановлен.
Следующий шаг — обычная партия founder'а на обновлённой странице; полного
прогона всех 118 аудиофайлов и проверки всех мобильных сетей не было.

`[FIX 15.09]` Телефон после исправления аудиовхода слышен: лог подтвердил
«Давай Д четыре», «Ну, Д четыре», «Д четыре» и play_move с правильным D4.
Новая intent-проверка отвергала кириллическую Д и разговорные вводные.
В `efb5dcb` добавлены формы букв, числовые строки и ограниченные вводные
перед отдельной координатой; обсуждение и отрицание по-прежнему запрещают ход.
Typecheck PASS, 175 целевых тестов PASS, включая три телефонные фразы через
инструмент до игрового клиента и запрет повторного исполнения. Опубликовано:
пять сервисов healthy, воркер зарегистрирован; те же три фразы проверены
через IntentLedger внутри контейнера, SHA-256 исходника совпал.
Полная голосовая партия после этого исправления ещё не подтверждена.

`[FIX 15.09]` После публикации founder сообщил, что агент не отвечает на голос.
В журнале было приветствие без входящих расшифровок: inputOptions.audioEnabled=false
полностью исключал создание RoomIO input, поэтому последующее включение было пустым.
В `d4d759d` поток создаётся всегда, а перед готовностью Live отключается через
session.input.setAudioEnabled(false). Регрессия на настоящем SDK сначала упала
с null input, после исправления прошла вместе с mode/clock: 13 tests; typecheck PASS.
Исправление опубликовано в рамках восстановления текущего обновления: пять сервисов
healthy, новый воркер зарегистрирован, SHA-256 исправления совпал внутри контейнера.
Последующий телефонный разговор подтвердил восстановление входящей речи;
обнаруженная при нём регрессия команд описана выше.

`[OK опубликовано]` Завершена вторая итерация после записи с телефона:
[явный разговор и GPT-Live](superpowers/specs/2026-09-15-goko-live-polish.md),
[план](superpowers/plans/2026-09-15-goko-live-polish.md). Один исполнитель
продукта за раз, независимое ревью, координатор ведёт исследования и QA.
Продукт e9063d1: явный запуск, общие часы записи, безопасный повторный вход,
GPT-Live с отдельным backend, защита намерения и ревизий, настоящие PV KataGo.
Финальный check: 1472 passed / 13 skipped; web build и независимое ревью PASS.
Движок отдельно прошёл 5 real contract tests, fake smoke и smoke --real.
Последний реальный прогон gpt-live-1/marin + gpt-5.6-luna прошёл: приветствие,
16 с тишины и away, typed get_position, одинаковый по смыслу backend/voice
ответ про E6, затем событие. Provider закрыт, usage.seconds=30.
Приёмка комнаты и микрофона телефона остаётся отдельной: разрешения,
кашель, настоящие перебивания, выразительность и длинная партия.
Известное ограничение SDK — replay написанного контекста при reconnect,
см. [D-0015](decisions/0015-explicit-live-conversation.md).
[Измерения и открытые проверки](research/2026-09-15-live-polish-acceptance.md).
15.09 по отдельному разрешению founder'а опубликован пакет `1273430`
с VOICE_MODE=live. Все пять сервисов healthy, воркер зарегистрирован,
SDK Agents/OpenAI 1.8.1 проверен внутри контейнера. Совпали 260 файлов
исходников и три публичных веб-артефакта; web/LiveKit HTTP200, KataGo healthy.
Во время сборки исправлен запуск download-files из workspace голосового агента.
Предыдущая конфигурация сохранена на сервере. Следующий шаг — телефонная приёмка.

15.09 founder поручил автономно реализовать улучшения после первой партии
и разрешил новую проверочную партию. Локально завершена итерация
[единого разговора, истории и записи](superpowers/specs/2026-09-15-goko-conversation-design.md)
по [плану](superpowers/plans/2026-09-15-goko-conversation.md), subagent-driven.
Тогда read-only проверка `gpt-live-1` дала HTTP200; последующие проверки
настоящего подключения описаны выше. Опубликованная первая итерация
использовала Realtime; опубликованная вторая итерация использует Live.
Запись первой версии — в браузере по кнопке, без серверного хранения.
Продуктовый HEAD `0382dc5`:1412tests passed,13skipped, smoke/build и независимые
ревью прошли. Одна живая партия с KataGo/cedar и записью проверена; найденная
потеря user STT исправлена и повторно проверена в той же сессии. Подробности —
[приёмка единого разговора](research/2026-09-15-conversation-acceptance.md).
15.09 по отдельному разрешению founder'а выполнен `deploy.sh --build-web`
из `d6765a3` (продукт `0382dc5`). Все пять сервисов healthy, голосовой воркер
зарегистрирован. Совпали 177 файлов исходников и три публичных веб-артефакта.
После публикации получена вторая телефонная запись: её разбор стал основанием
текущей итерации. Новая версия опубликована после локальных проверок и отдельного разрешения.

Стадия 1: код планов `superpowers/plans/2026-09-07-goko-stage1-core.md` и
`superpowers/plans/2026-09-07-goko-stage1-voice-web.md` готов (voice-agent,
web, контейнеры, runbook) и развёрнут на VPS; текущая версия `eb518f6`. Браузерная проверка
2 пройдена 12/12, широкое ревью и финальный пакет исправлений закрыты. Стадия
не завершена: бесплатные проверки после деплоя прошли; первый голосовой
прогон founder'а выявил обрывы, излишнюю краткость и слабые объяснения.
Разбор и предложение единого интерфейса с записью для диагностики — в
[`research/2026-09-14-voice-feedback.md`](research/2026-09-14-voice-feedback.md).
Нужно устранить дефекты, завершить телефонную приёмку, проверить контроль
расходов OpenAI и замерить стоимость партии по
[`research/2026-09-14-stage1-acceptance.md`](research/2026-09-14-stage1-acceptance.md).

Ветка `stage0` слита в `main` 14.09 после широкого ревью; исходный checkout
сохранён на `eb32633`, финальный пакет продолжается в `codex/stage1-final`.

Стадия 0 закрыта по всем задачам, которые агент может выполнить без микрофона.
Результаты — `research/stage0-results.md`, ход задач — `journal/2026-09-08-stage0.md`.
Первый голосовой прогон состоялся; полная матрица сетей, фраз и стоимости
ещё не закрыта.

Планы реализации лежат в `docs/superpowers/plans/`:

- `2026-09-07-goko-stage0-spike.md` — стадия 0: `infra/`, стартовый агент,
  замеры KataGo, фразы, текстовый канал, `docs/research/stage0-results.md`.
- `2026-09-07-goko-stage1-core.md` — стадия 1, ядро: `go-core`, `protocol`,
  `go-engine`, `game-server`, `smoke`, `dev` (13 задач).
- `2026-09-07-goko-stage1-voice-web.md` — стадия 1, голос и веб:
  `voice-agent`, `scripts/chat.mjs`, `web`, контейнеры, runbook (11 задач).

Корпус голосовых проверок: `spike/phrases.md`, 118 фраз с шести записей
founder'а, разрезаны по словам в `data/voice/phrases/` (вне git — голос
founder'а, репозиторий публичный).

## Инфраструктура `[OK]` (08–14.09)

- VPS `goko`: Hetzner cx23 (2 vCPU, 4 ГБ, 40 ГБ, AVX2 есть), Хельсинки,
  Ubuntu 24.04, отдельный проект Hetzner `goko`; SSH-алиас `goko` на ПК
  founder'а. Токен API Hetzner — в `.env` на ПК как `HETZNER_API`, не в git.
- Домены: `goko.sdamex.com` (страница и `/api`) и `goko-lk.sdamex.com`
  (LiveKit), A-записи в Yandex Cloud DNS зоны `sdamex.com`, TTL 300;
  правятся с ПК founder'а через `yc dns zone list-records / add-records`.
  В `.env` это `WEB_HOST` и `LK_HOST`.
- Подготовлено 08.09: Docker, ufw (в том числе relay-порты TURN 30000-40000/udp),
  `/opt/goko` с `.env` режима 600, Caddy и LiveKit `v1.13.6` в compose,
  сертификаты Let's Encrypt до 07.12.2026.
- 14.09 выполнен первый деплой стадии 1 из `2e6358d`: web собран на VPS из
  git-архива этого SHA, три SHA-256 артефакта и 223 хеша исходников совпали.
  Через 45 с все пять сервисов healthy, воркер зарегистрирован, движок healthy
  (`queue=0`, `restarts=0`), web и LiveKit отвечают 200. На VPS подтверждены
  `MAX_SESSIONS=8`, `TRUST_PROXY=1` и ротация 10 МБ × 3 у всех пяти сервисов.
  Разрешение founder'а на один `deploy.sh` использовано; следующий деплой
  требует нового разрешения.
- Ключи LiveKit сгенерированы на сервере, ключ OpenAI вписан в `/opt/goko/.env`.
- Репозиторий `github.com/emevart/goko`, публичный, лицензия MIT.
- KataGo на ПК: бинарь OpenCL v1.18.1 и обе сети скачаны, `KATAGO_BIN` в `.env`.

## Открытые вопросы founder'у `[TODO founder]`

1. **`[OK]` Ключ OpenAI перевыпущен** founder'ом 08.09 и разложен в `.env` на
   ПК и на сервере, доступ проверен живым запросом. Инцидент закрыт.
2. **Повторная приёмка после замечаний первого голосового прогона — блокер
   стадии 1** (шаг 6 «Следующего шага»). Первый прогон 14.09 состоялся,
   но голос и объяснения требуют правок. Перед следующим founder проверяет в OpenAI
   `Enforce a hard limit` и отдельные alerts: фактическая настройка аккаунта
   ещё не подтверждена. Пошаговая инструкция — в
   [`research/2026-09-14-stage1-acceptance.md`](research/2026-09-14-stage1-acceptance.md).
   Одна сессия закрывает и приёмку стадии 1, и четыре пустых
   места в `research/stage0-results.md`: проходит ли медиапоток из сотовой сети
   и из дома без VPN, доля распознанных координат из 45 фраз
   (`spike/phrases.md`), качество русского голоса Realtime и перебивание,
   стоимость минуты. Только на телефоне проверяются пункты, которые не прошли
   на ПК: запрос микрофона в Safari/Chrome, системный индикатор, запрет
   хранилища, экран не гаснет, голос (режим «Голос», «дэ четыре», озвученные
   ответы, задержка ответа, разборчивость координат), реплика на тап в «Голосе»
   звучит (D-0013), голосовой ход сразу после ответа на тап вызывает
   `play_move` (`toolChoice` вернулся в `auto`), перезагрузка в «Голосе»,
   режим «Чат» без звука. По итогу — realtime или pipeline по цене партии (в
   «Чате» Realtime всё равно генерирует аудио — цена D-0011). В пункте «консоль
   без ошибок» строки Chrome «Failed to load resource» на ожидаемые ответы
   400/409/502 — не ошибка приложения. Если поток душат — RU-релей или LiveKit
   на RU-VPS, раздел 11 спеки.
3. **`[OK]` Почта для Let's Encrypt вписана** founder'ом 09.09: `ACME_EMAIL` в
   `/opt/goko/.env` заполнен, контейнер Caddy пересоздан, оба хоста отвечают 200.
   Значение в git не попадает. Текущие сертификаты выписаны на аккаунт ACME без
   почты; новый аккаунт с почтой Caddy заведёт при продлении (до 07.12.2026).
4. **Tailscale** нужен только для dev-режима стадии 1 (Caddy на VPS →
   game-server на ПК). Когда дойдёт: агент выполнит `tailscale up` на VPS и
   пришлёт ссылку для входа, founder откроет её и подтвердит узел.
5. **`[OK]` Правки спеки 07.09 подтверждены** founder'ом 13.09: отдельный VPS вместо LiveKit Cloud на
   стадии 0; личный VPN-сервер не используем; запасной конвейер STT → LLM →
   TTS; раздел 18 про AI-first; коды ошибок и причины `state.updated`.
6. **`[OK]` Способ исполнения стадии 1** выбран 08.09: субагентами, начиная с
   плана ядра.
7. **Две неоднозначные фразы** в `spike/phrases.md`: строка 61 «у меня всё ок» и
   строка 110 «как ходить». Ожидание проставлено догадкой; если founder помнит,
   что имел в виду, поправим на настоящее. Работу не держит, вернёмся позже.

## Открыто

- `[TODO]` Первое изменение после приёмки: сессия без потока SSE и участника
  должна истекать раньше, чтобы брошенная вкладка быстрее освобождала слот
  `MAX_SESSIONS` (I2).
- `[TODO]` После ранней очистки — «вариант C»: эндпоинт
  game-server для повторного вызова агента в ту же комнату (re-dispatch), если
  агент ушёл (телефон не вернулся за 15 минут: `RETURN_GRACE_MS` воркера и
  `departureTimeout` 900 с комнаты).
- `[OK]` Проверка страницей в браузере на ПК пройдена 12 из 12: четыре бесплатных
  пункта без воркера и восемь пунктов с агентом в «Чате». Вход сразу в «Чате»,
  текстовые ответы, тапы, «Гоко вышел» и перезагрузка проходят; текстовый ход
  «сыграй …» сразу после реплики на тап применён, значит `toolChoice` вернулся
  в `auto`. Круг правок 2 задачи 8 (сторож простоя SSE 45 с в `protocol`,
  подсказка «Гоко не пришёл», защита от второго тапа, строка «Повторить»)
  закоммичен, идёт его ревью. Два дефекта исправлены 14.09 (D-0013): воркер
  сообщает загрузку по числу job, а не по CPU машины; реплики событий идут
  фактом «Событие с экрана» в историю разговора и отвечаются без инструментов.
  Платный прогон в «Чате» (7 тапов, из них 2 быстрых подряд): ни одного
  `play_move` на событие, координата ответа Гоко в каждой реплике. Пункты
  только для телефона — открытый вопрос 2. Круг правок 1 по ревью (D-0013
  обновлено): воркер держит один тёплый процесс job — без него в dev
  завершённые job оставались в счёте, и после шестой комнаты агент не
  приходил (опыт без участника: было 6 из 8 комнат, стало 8 из 8); реплика
  события копирует историю Realtime только после выхода модели из `thinking`
  (потолок 20 с), чтобы событие и результат инструмента не удаляли друг друга.
  Круг правок 1 проверен тестами и бесплатным опытом, платного прогона не было:
  на тапах без голосового хода путь реплики не изменился.
- `[WIP]` Платные прогоны плана голоса и веба: бюджет — 10 (founder расширил
  его 14.09). Израсходовано 8, остаток 2 — только на проверку правок и
  воспроизведение, для которых нет бесплатного пути; длительность каждого
  прогона записывается. Пятый — расследование дефектов (Realtime около 40 с),
  шестой — проверка правок D-0013 (сеанс 61 с, агент в комнате 57 с), седьмой —
  браузерная проверка 2 (две сессии Realtime, всего 24 мин 1 с; активная часть
  около 8 мин 40 с). Первые три — меньше $1 вместе; в
  четвёртом Realtime был открыт около 17 минут, активная часть около 2,5
  минут. Деплой и его бесплатные проверки новых платных прогонов не добавили;
  денежная стоимость не измерена. Восьмой прогон15.09: одна партия/сессия,
  около10мин6с с ожиданием исправления, запись128с; свои процессы остановлены.
  Итог — в отчёте приёмки.
- `[TODO]` Замер стоимости партии: сколько входных токенов добавляют
  сообщения «Событие с экрана» в истории Realtime (оценка ревью — 3–6 тыс. на
  партию, и они входят в каждый следующий ответ). Если рост заметен — удалять
  события старше N ходов (`conversation.item.delete`) в той же очереди реплик.
- `[TODO]` После приёмки: ограничить запросы воркера по `sessionId`, а не только
  общим лимитом адреса (M1).
- `[TODO]` После приёмки: перевыпускать токен LiveKit для ещё живой сессии,
  пережившей фиксированные 2 часа токена (M5).
- `[TODO]` После приёмки: вынести `vite.config` в `tsconfig.node.json` и убрать
  `types: ["node"]` из браузерного `src` (M9).
- `[TODO]` После приёмки: в `scripts/chat.mjs` дождаться записи `console.error`
  перед `process.exit` и учитывать `CHAT_RUN_MAX_MS` во время ожидания stdin
  (M12).
- `[TODO]` После приёмки: закрыть окна event-speech N1/N2 — зависшее `thinking`
  после прерванного ответа инструмента и обратную гонку сверки истории.
- `[TODO]` После приёмки: при возврате телефона брать режим из нового объекта
  участника; сейчас в редком окне 1–3 с читается старый объект (`main.ts`).
- `[TODO]` После приёмки: сохранять ленту разговора при перезагрузке вкладки.
- `[!]` При обновлении `@livekit/agents`: проверить по исходникам, что
  `agent._chatCtx` (помечен `@internal`) и вставка в него работают как в 1.8,
  что `agentState` держит `thinking` на время выполнения инструмента и что
  пул процессов с `numIdleProcesses: 1` убирает завершённые job; прогнать
  тесты `event-speech` и `load` и опыт из D-0013 (8 сессий без участника).
- `[OK]` После деплоя SSE через Caddy отдал пинги отдельно через 15,0 и 30,0 с:
  `flush_interval -1` работает.
- `[OK]` Комната без человека закрылась штатно по `empty_timeout`: создана в
  20:17:20.684Z, закрытие началось в 20:22:20.495Z (через 299,8 с), LiveKit
  указал причину `empty timeout`; в 20:22:56.046Z `listRooms` подтвердил
  отсутствие комнаты. Воркер записал `[!] voice-agent: участник не пришёл,
  комната закрыта`. Серверная сессия остаётся и занимает один слот до TTL; при
  лимите 8 рестарт для её удаления не нужен.
- `[OK]` Живые проверки 3 и 4 runbook: с VPS ровно 60 запросов вернули 400,
  следующий — 429; одновременный запрос с ПК вернул 400, значит rate limit
  считает клиентов независимо. `POST /api/games` в prod вернул ожидаемый
  `sessionless_disabled` (400).
- `[!]` Токен LiveKit живёт фиксированные 2 часа от выдачи (D-0008), а
  продлённая событиями сессия может его пережить. После отказа входа страница
  забывает старую сессию, создаёт новую, а следующее касание входит с новым
  токеном. Если на приёмке это мешает — выпуск нового токена на живую сессию,
  отдельное решение.
- `[TODO]` Удалить `spike/` и вычистить его из `tsconfig.json` и `workspaces`
  (задача 8 стадии 0) — после прогона голосом, не раньше.
- `[TODO]` doctor: группа проверок `voiceAgent` вместо `spike` и проверка
  `VOICE_MODE` без печати значения — открытый пункт, код не менялся.
- `[TODO]` Стадия 2: текст ответа 429 `too_many_games` при возврате к партии
  или действии в ней («новую можно начать позже») не про возврат — уточнить
  формулировку; `MAX_SESSIONS` общий на сервер, не на клиента.
- `[TODO]` Детям `smoke` и `dev` уходит всё окружение, кроме `LIVEKIT_*` и
  `OPENAI_*` в smoke (например, ключ API облака). Значения не печатаются;
  белый список переменных по принципу наименьших прав не сделан.
- `[TODO]` POSIX-ветки `smoke` и `dev` (дети в своих группах процессов, дерево
  гасится `process.kill(-pid)`) ни разу не прогонялись: проверить на Linux или
  macOS.
- `[FIX]` Ctrl+C в `npm run dev` для `web` (14.09, задача 11): через `cmd.exe`
  (`shell: true`) вопрос «Завершить выполнение пакетного файла?» появлялся;
  stdin у детей `ignore`, поэтому web всё же выходил, остановка 0,7 с. Правка
  `scripts/dev.mjs`: web запускается как `node node_modules/vite/bin/vite.js
  apps/web` без оболочки, тест в `scripts/dev.test.ts`. После правки вопроса
  нет, `[OK] dev: всё остановлено` за 1,0 с, порты 8787, 8788, 5173 свободны.
  Проверено настоящим `CTRL_C_EVENT` в отдельной консоли с реальным `.env`
  (воркер `goko-dev` регистрируется, в комнату никто не входит).
- `[!]` `dev` на Windows: дети получают Ctrl+C одновременно с `dev`. Если все
  они выйдут раньше, чем сработает обработчик SIGINT в `dev`, `dev` напечатает
  `[X] dev: все процессы завершились` и выйдет с кодом 1. В замере обработчик
  успел первым; код не меняли.
- `[OK]` Блокер публичного деплоя (широкое ревью, R9): лимит незавершённых
  партий на клиента и партии в prod только внутри сессии — задача 9 плана
  голоса и веба, D-0012.
- `[OK]` План голоса и веба (широкое ревью, R2): опрос `get_game` не чаще раза
  в 2,5 с и `blockedUntil` по `Retry-After` — задача 2; растущая пауза,
  `Retry-After` и выход на `not_found` в `watchSession` — задача 3; серверная
  квота агента — расчёт в задаче 10, отдельного ключа нет.
- `[OK]` Снапшоты брошенных незавершённых партий с диска сами не удаляются:
  чистка руками — `docs/runbooks/vps.md`, раздел «Брошенные незавершённые
  партии»; комментарий к `SESSION_TTL_MS` в `infra/.env.example` дописан
  (порог брошенной партии).
- `[TODO]` Отложено: комната LiveKit остаётся после таймаута `createRoom`.
- `[TODO]` Общий таймаут прогона в CI (решение задачи 10), когда появится CI.
- `[!]` Косметика: лог stderr KataGo рвётся посреди строки (префикс на кусок).

## Следующий шаг

Стадия 1 — по порядку, следующий пункт не начинается до закрытия предыдущего:

1. `[OK]` Расследование двух дефектов живой проверки на ПК: первая сессия
   после старта воркера осталась без агента; реплики Гоко на тапы не про
   позицию.
2. `[OK]` Правки по итогам расследования (D-0013) и браузерная проверка 2:
   12/12, включая «Гоко вышел», перезагрузку в «Чате» и текстовый ход сразу
   после реплики на тап.
3. `[OK]` Широкое ревью ветки `stage0`, финальный пакет замечаний и короткое
   независимое ревью.
4. `[OK]` Первый деплой стадии 1 из `2e6358d`; одно разрешение на `deploy.sh`
   использовано.
5. `[OK]` Бесплатные проверки после деплоя из
   [`runbooks/vps.md`](runbooks/vps.md): health, хеши, SSE, rate limit,
   `sessionless_disabled` и `empty_timeout`.
6. `[WIP]` Первый голосовой сценарий состоялся: 13×13, 28 ходов в итоговой
   ветке, смена ранга и сдача. По отзыву и логам — обрывы, краткость и слабые
   объяснения. Итерация единого интерфейса, redo и записи локально готова,
   проверена одной новой партией; результаты — в отчёте15.09 выше.
   Обновление выложено из `d6765a3` по отдельному разрешению 15.09.
   Вторая запись разобрана, пакет e9063d1 с Live локально проверен. Следующий
   шаг: отдельное разрешение на новый deploy с VOICE_MODE=live, затем повторная
   телефонная приёмка. Проверка hard limit и alerts открыта.
7. `[TODO founder]` Выгрузить Billing Usage и записать стоимость партии; по
   длительности её не вычислять.

После приёмки первым сделать раннее истечение сессии без потока и участника,
затем остальные пункты раздела «Открыто» и brainstorming стадии 2 (место
`external`, `mcp-server`).

## Сделано

- 2026-09-07: brainstorming, спека v1, правки по инфраструктуре и речи,
  `CLAUDE.md`, `AGENTS.md`, `README.md`, карта документации.
- 2026-09-07: планы реализации стадии 0 и стадии 1 (ядро; голос и веб);
  в спеке уточнены коды ошибок и причины `state.updated`.
- 2026-09-08: организация работы через субагентов (`process/orchestration.md`),
  каркас монорепы и `doctor` (задача 1), внешняя верификация планов до кода.
- 2026-09-08: VPS `goko` (cx23) и A-записи `goko.`/`goko-lk.` в `sdamex.com`;
  LICENSE MIT, публикация репозитория; в планах и спеке `WEB_HOST`/`LK_HOST`
  вместо `DOMAIN`, cx23/cx33 вместо CX22/CX32, риск троттлинга и запасной
  RU-релей в разделе 11.
- 2026-09-08: широкий обзор ветки `stage0`, шесть исправлений, ре-ревью чисто;
  исправленная страница спайка выкачена. Предполётная сверка плана ядра до
  написания кода: шесть блокирующих дефектов плана и девятнадцать замечаний,
  решения вынесены, правки в план идут.
- 2026-09-08: стадия 0, задачи 1-5 и 7. `infra/` и подъём VPS (Docker, ufw,
  Caddy, LiveKit, TLS), образ движка и замер KataGo на сервере, агент-спайк на
  `gpt-realtime` с выкаткой и живой комнатой, текстовый канал `chat.mjs`,
  `research/stage0-results.md` с решениями. В спеке зафиксированы основная
  сеть `b10c128`, тариф cx23 без апгрейда, режим голоса `realtime` с
  параметрами `server_vad`, relay-порты TURN. Задача 6 (стоимость и запасной
  конвейер) на паузе до голосового прогона.
- 2026-09-13: стадия 1, ядро закрыто — задачи 8 и 10 после ре-ревью, 11
  (сессии, токены LiveKit, D-0001), 12 (HTTP, SSE, запуск), пакет правок 12b,
  13 (`npm run smoke`, `npm run dev`, сверка спеки с кодом, D-0002…D-0010).
- 2026-09-13: ревью задачи 13 и правки по нему. Починены остановка game-server
  при медленном клиенте SSE и пустые переменные env. Ctrl+C в `npm run dev` на
  Windows теперь доходит до детей. `smoke` не ждёт `/health` у вышедшего
  процесса, а Ctrl+C в `smoke` считается ошибкой и дожидается остановки детей.
  go-engine отклоняет неверный `ENGINE_PORT`. Уточнены спека (в том числе
  остановка game-server со счётчиком запросов), D-0001, D-0005, D-0006 и D-0010.
- 2026-09-14: правки по широкому ревью ветки `stage0`. Бюджеты движка, клиента
  и сервиса выстроены по правилу «движок < клиент < сервис» с отменой по
  дедлайну; поток сессии несёт события только текущей партии; таймауты
  клиента протокола; лимиты частоты, числа партий и срок хранения снапшотов
  (D-0012); полуцелое коми, место в `resign`, свободный id (D-0009), долговечная
  запись снапшота, ключ go-engine за постоянное время. `doctor` читает `.env`
  как `loadEnvFile`, помощники тестов — по подпути `./testing`. Спека, D-0009,
  D-0010, README (лицензия MIT).
- 2026-09-14: код плана голоса и веба стадии 1: voice-agent (девять инструментов, промпт,
  события, realtime/pipeline, режимы Голос/Чат по D-0011), chat из консоли, web
  (доска, лента-диалог, микрофон, чат, выбор цвета и ранга, «Повторить»),
  контейнеры и compose, runbook VPS.
- 2026-09-14: исправления двух дефектов живой проверки (D-0013). Загрузка
  воркера — по числу job, потолок 6 одновременных. Реплики событий — фактом
  «Событие с экрана» в историю Realtime с ожиданием подтверждения сервера, ответ
  `generateReply` с `toolChoice: 'none'`. Тексты событий — «ход уже на доске,
  скажи вслух только: …». Правило промпта про события, приветствие без «твой
  ход». Спека, реестр решений, платный прогон 6 из 10.
- 2026-09-14: браузерная проверка 2 пройдена 12/12; финальное ревью закрыто.
  Первый деплой стадии 1 из `2e6358d` успешен, базовые health и конфигурация
  подтверждены. Бесплатные проверки SSE, rate limit, запрета партии без сессии
  и закрытия пустой комнаты прошли. Ход работ — в
  [`journal/2026-09-14-stage1-voice-web.md`](journal/2026-09-14-stage1-voice-web.md).
