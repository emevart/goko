---
status: implemented
area: plan
updated: 2026-09-16
---

# План живого 3D-орба

Основание: [спецификация живого орба](../specs/2026-09-16-orb-visual-design.md).
Это новый интерфейсный слой; код по плану начат после явного подтверждения
founder'а «поехали, inline».

1. [x] Зафиксировать контракт `OrbState`, отдельные `micLevel`/`agentLevel` и
   таблицу приоритетов состояний; добавить чистые unit-тесты.
2. [x] Реализовать CSS fallback и лениво загружаемый Three.js renderer с корректным
   dispose, reduced-motion и ограничением pixel ratio.
3. [x] Добавить цельную shader sphere с органической деформацией, объёмным
   освещением, Fresnel shell без внешних линий и сглаженными переходами через
   интерполяцию.
4. [x] Подключить реальные атрибуты LiveKit и аудиоуровни; проверить, что `tool`
   виден отдельно от `thinking`, а `speaking` реагирует на голос Гоко.
5. [WIP] Проверить все браузерные состояния, мобильный viewport, WebGL fallback и
   производительность; локально подтверждены запуск сцены в desktop/mobile и
   CSS-слой при подключении, полная матрица состояний требует живой сессии.
6. После отдельного разрешения на публикацию собрать web, пройти полный check и
   подготовить релиз.
