---
status: living
area: process
updated: 2026-09-14
---

# Журнал: стадия 1, голос и веб

План: `superpowers/plans/2026-09-07-goko-stage1-voice-web.md`. Реализация
начата в `stage0`, финальный пакет и деплой — `codex/stage1-final`.
Механический журнал координатора лежит в `.superpowers/sdd/` и в git не
попадает. Текущий порядок работ — в [`NOW.md`](../NOW.md), операционные
команды — в [`runbooks/vps.md`](../runbooks/vps.md), результаты телефона и
расходов — в
[`research/2026-09-14-stage1-acceptance.md`](../research/2026-09-14-stage1-acceptance.md).

## Реализация и проверка

- Реализованы 11 задач плана: voice-agent, чат из консоли, web, контейнеры,
  compose и runbook.
- Браузерная проверка 2 пройдена 12/12. Платный прогон 7 из 10 подтвердил
  реплики на тапы, отсутствие лишних ходов, возврат `toolChoice` в `auto`,
  перезагрузку в «Чате» и уход агента.
- Широкое ревью и финальный пакет закрыты коммитами `f4caade`, `eb32633`,
  `6ffd222`, `2e6358d`; независимое ревью дало PASS после одной редакционной
  правки `NOW.md`.
- Общая проверка перед деплоем: typecheck, 1350 тестов (13 skipped), smoke и
  doctor прошли; compose-конфигурация на VPS валидна.

## Первый деплой стадии 1

- 14.09 один разрешённый `deploy.sh` успешно выполнен из `2e6358d`; web собран
  на VPS из git-архива того же SHA. Разрешение израсходовано.
- Три SHA-256 артефакта и 223 хеша исходников совпали. Через 45 с все пять
  сервисов healthy; воркер зарегистрирован, движок без очереди и рестартов,
  web и LiveKit отвечают 200.
- Подтверждены `MAX_SESSIONS=8`, `TRUST_PROXY=1`, ротация 10 МБ × 3 у всех
  сервисов, отдельные пинги SSE и запрет партии без сессии.
- Rate limit считает клиентов независимо: с VPS 60 ответов 400 и следующий
  429, одновременно с ПК — 400. Пустая комната начала закрываться штатно через
  299,8 с по `empty_timeout`; через 335,3 с API подтвердил её отсутствие,
  воркер — что участник не пришёл. Серверная сессия занимает один слот до TTL,
  но при лимите 8 рестарт не нужен.
- Платных прогонов при деплое не было; исторический счёт 7/10, остаток 3.
  Денежная стоимость не измерена.

## Следующий шаг

Founder проверяет в аккаунте OpenAI `Enforce a hard limit` и alerts, выполняет
один голосовой прогон по отчёту приёмки и записывает стоимость из Billing
Usage. После приёмки первое изменение — раннее истечение сессии без потока SSE
и участника.
